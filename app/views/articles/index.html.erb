<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= link_to article.title, article %></td>
      <td><%= article.body %></td>
    </tr>
  <% end %>

</table>

<%= link_to 'New article', new_article_path %>